<h2>More details on <%= @tag.name %></h2>

<p>
  You have spent £<%= @tag.total_spend %> on <%= @tag.name %>.
  On average, you spend y amount here.
  The most common place you buy this is z.
</p>

<table>
  <tr>
    <th>Merchant</th>
    <th>Spent</th>
  </tr>
  <% for transaction in @tag.all_transactions %>
    <tr>
      <td> <%= transaction.show_merchant_name %></td>
      <td> £<%= transaction.spend %> </td>
  </tr>
  <% end %>
</table>


<form method="post" action="/spending-tracker/tags/<%=@tag.id%>/delete">
<input type="submit" value="Delete"/>
</form><p>
  Warning! Deleting tag data will delete all transactions associated with this tag. This action cannot be undone!!</p>
